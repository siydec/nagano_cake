<div class="container mt-3">
  <h2 class="mt-5">ログイン</h2>
  <div class="row">

    <div class="col-5 mt-5">
      <p>会員の方はこちらからログイン</p>
      <%= form_with(model: resource,scope: resource_name, url: session_path(resource_name)) do |f| %>
      <div>
        <table class="table table-borderless">
           <tr>
             <th><%= f.label :メールアドレス %></th>
             <td><%= f.email_field :email, autocomplete: "email" %></td>
           </tr>
           <tr>
             <th><%= f.label :パスワード %></th>
             <td> <%= f.password_field :password, autocomplete: "current-password" %></td>
           </tr>
           <tr><td><%= f.submit "ログイン", class:"btn btn-primary" %></td></tr>
        </table>
        </div>
      <% end %>
    </div>
  </div>
  <p class="mt-3">会員登録がお済でない方</p>
  <div class="row">

      <p>
      <%= link_to controller:"public/registrations", action:"new" do %>
      <%= "こちら" %>
      <% end %>から新規登録を行ってください
      </p>
    </div>
</div>